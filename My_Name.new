import sopel.module

@sopel.module.rate(120)
@sopel.module.commands(botnickname)
def name(bot,trigger):
    bot.say("That's my name. Don't wear it out!")

def botnickname(bot):
    botnickname = bot.nick
    return botnickname

botnickname = botnickname(bot)
